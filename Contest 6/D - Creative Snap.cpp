#include <bits/stdc++.h>
using namespace std;

int comprimento, nAvengers, A, B;
long long poderMin;

int destroi(){
    
}
 
int main(){
    ios_base::sync_with_stdio(false);
    vector<bool> ocupadas;
    int loop, a;

    cin >> comprimento >> nAvengers >> A >> B;

    for(loop=0; loop<nAvengers; loop++){
        cin >> a;
        ocupadas.push_back(a);
    }


    
    
    return 0;
}
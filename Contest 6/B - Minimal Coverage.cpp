#include <bits/stdc++.h>
using namespace std;
 
int main(){
    ios_base::sync_with_stdio(false);
    int testes, fimSegmento, a, b, loop;
    vector<pair<int, int>> linhas;

    cin >> testes;
    for(loop=0; loop<testes; loop++){
        linhas.clear(); a=1; b=1;
        cin >> fimSegmento >> fimSegmento;
        while(a!=0 || b=!0){
            cin >> a >> b;
            linhas.push_back({a, b});
        }



    }


    
    
    return 0;
}